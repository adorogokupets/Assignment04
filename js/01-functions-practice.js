//STEP 1
function halfNumber(number){
    let result = number / 2
    console.log(`Half of ${number} is ${result}.`)
    return result
}

halfNumber(5)


//STEP 2
function squareNumber(number){
    let result = number * number
    console.log(`The result of squaring the number ${number} is ${result}.`)
    return result
}

squareNumber(3)

//STEP 3
function percentOf(percent, number){
    result = percent * number / 100
    console.log(`${result} is ${percent}% of ${number}.`)
    return result
}

percentOf(30, 9)

//STEP 4
function findModulus(number1, number2){
    result = number1 % number2
    console.log(`${result} is the modulus of ${number1} and ${number2}.`)
    return result

}

findModulus(10, 4)

//STEP 5

// It were only 4 steps in Practice with Functions